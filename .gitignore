node_modules/
.next/
out/
.DS_Store
*.log
.env*.local
firebase-debug.log
.firebase/
next-env.d.ts
tsconfig.tsbuildinfo
/shots/
_to_delete/
.simbuild/

# Playtest screenshots. tools/*-shots.mjs and tools/cap-*.mjs write here, and
# they are proof for one change, not a record worth carrying: the same folder
# was cleared of 15MB in September. Run the tool again when you need them.
docs/*.png
# The home-screen icon preview is the one PNG this folder is meant to keep.
!docs/app-icon-preview.png
# Energy Attack / Energy Shield phone proof for the 25 September 2026 request.
!docs/energy-weapons-*.png
.iconbuild/
